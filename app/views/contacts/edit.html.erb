<div id='new_contact_form'>
  <%= simple_form_for [@user, @contact] do |f| %>
    <%= f.input :full_name %>
    <%= f.input :phone_number %>
    <%= f.submit %>
  <% end %>
  <div style='margin-top:30px;margin-left:110px;'>
    <%= link_to "Back", contact_path(@contact), class: "btn btn-primary", id:"go_to_btn"%>
  </div>
</div>
